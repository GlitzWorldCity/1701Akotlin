package com.example.myapplication.data.db

/**
 * 1、定义了contacttable类
 * 2、创建了ContactTable的一个实例，通过类名，之间访问实例，实现单例的一种方式
 */
object ContactTable{
    val NANME = "contact"
    val ID = "_id"
    val CONTACT = "name"
}